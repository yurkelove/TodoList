import React from "react";


const EmptyList = () => {

        return (

            <div className={`message__container`}>
                <p className={`show__message`}>Add items to the list,because your list is empty</p>
            </div>

        )
};


export default EmptyList